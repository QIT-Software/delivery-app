export enum EmailValidationResult {
  invalid,
}

export enum ConfirmPasswordMatchResult {
  notMatch,
}

export enum PhoneValidationResult {
  doesNotMatch,
}

export enum NumberValidationResult {
  notNumber,
}

export enum LengthValidationResult {
  tooShort,
  tooLong,
}

export enum MaskInputValidationResult {
  invalid,
}

export enum TimeValidationResult {
  nonExistentTime,
}

export enum WorkHoursValidationResult {
  nonWorkingHours,
}

export enum NameValidationResult {
  inappropriateCharacters,
}

export enum EmptyStringResult {
  empty,
}

export enum ExistsResult {
  notExists,
}

export enum BirthdayValidationResult {
  invalid,
}

export enum PayPalValidationResult {
  invalid,
}
