import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  addressContainer: {
    borderBottomWidth: 1,
    borderColor: '#EDEDED',
  },
  addressText: {
    fontStyle: 'normal',
    fontWeight: '500',
    fontSize: 20,
    lineHeight: 24,
    paddingVertical: 12,
  },
});

export default styles;
